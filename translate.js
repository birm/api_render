// given a document and rules,

// for key in rules top level, try to get something from document matching
// if match, what's the rule?
// if it's a string, render out
// if it's a document, then recurse the match in doc with simplified rule set

function translate(doc, rules){
  
}
